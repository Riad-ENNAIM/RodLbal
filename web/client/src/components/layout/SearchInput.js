import React from 'react';

const SearchInput = () => {
  return (
    <input 
      id="searchInput"
      placeholder="Search a region ..."
      autoComplete="off"
    />
  );
}

export default SearchInput;